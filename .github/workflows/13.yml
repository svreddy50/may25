name: Tags-Functions-1
on: 
  workflow_dispatch:
  push: 
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      run: |
         echo "Hello Checkout"
         echo "${{ github.ref }}"
    - name: Build
      run: |
        hostnamectl
    - name: Deploy
      if: startsWith(github.ref , 'refs/tags/rel-')
      run: |
           echo " Deployment is Sucessful"  
               
       