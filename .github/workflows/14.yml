name: wf-matrix
on: workflow_dispatch
jobs:
  jobx:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
        - ubuntu-latest
        - window-latest
    steps:
    - name: print msg
      shell: bash
      run: |
         echo "This is ${{ runner.os }}"
